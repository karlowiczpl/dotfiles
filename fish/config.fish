if status is-interactive
    # Ustawienie ścieżki do SDK
    set -Ux PICO_SDK_PATH /home/Karol/repozytoria/pico/sdk/

    # # Dodanie SDK do PATH, jeśli potrzebne
    # set -x PATH $PICO_SDK_PATH $PATH
end
